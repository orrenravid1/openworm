TITLE Mod file for component: Component(id=neuron_to_neuron_elec_syn type=gapJunction)

COMMENT

    This NEURON file has been generated by org.neuroml.export (see https://github.com/NeuroML/org.neuroml.export)
         org.neuroml.export  v1.11.0
         org.neuroml.model   v1.11.0
         jLEMS               v0.12.0

ENDCOMMENT

NEURON {
    POINT_PROCESS neuron_to_neuron_elec_syn
    RANGE weight                            : property
    RANGE conductance                       : parameter
    RANGE i                                 : exposure
    
    
    NONSPECIFIC_CURRENT i 
    POINTER vpeer: derived variable as pointer...
    
}

UNITS {
    
    (nA) = (nanoamp)
    (uA) = (microamp)
    (mA) = (milliamp)
    (A) = (amp)
    (mV) = (millivolt)
    (mS) = (millisiemens)
    (uS) = (microsiemens)
    (nF) = (nanofarad)
    (molar) = (1/liter)
    (kHz) = (kilohertz)
    (mM) = (millimolar)
    (um) = (micrometer)
    (umol) = (micromole)
    (pC) = (picocoulomb)
    (S) = (siemens)
    
}

PARAMETER {
    
    weight = 1
    conductance = 5.0E-7 (uS)              : was: 5.000000000000001E-13 (conductance)
}

ASSIGNED {
    ? Standard Assigned variables with baseSynapse
    v (mV)
    celsius (degC)
    temperature (K)
    vpeer (mV)                              : derived variable
    i : no units???                         : derived variable
    
}

STATE {
    
}

INITIAL {
    temperature = celsius + 273.15
    
    rates()
    rates() ? To ensure correct initialisation.
    
}

BREAKPOINT {
    
    rates()
    
}

PROCEDURE rates() {
    
    ? DerivedVariable is based on path: peer/v, on: Component(id=neuron_to_neuron_elec_syn type=gapJunction), from peer; null
    ? Derived variable: vpeer; its value will be set by a pointer...
    
    i = -1 * weight  *  conductance  * (vpeer - v) ? evaluable
    
     
    
}

